# Local environment variables for running tests in development.
# Copy this file to `tests/.env` and edit it with your credentials.
# WARNING: Do NOT commit real secrets to source control. Use CI secrets for GitHub Actions.

# TIDAL client credentials for integration tests
TIDAL_CLIENT_ID=
TIDAL_CLIENT_SECRET=

# Optional: override token URL for testing against a mock or staging server
#TIDAL_TOKEN_URL=
